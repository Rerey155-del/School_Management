services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      # ini agar hot-reload Vue (Vite) berjalan lancar di dalam Docker Windows
      - CHOKIDAR_USEPOLLING=true
